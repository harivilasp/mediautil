# mediautil-cli

PyPI launcher package for the `mediautil` Rust binary.

`mediautil` is a local-first CLI for image/PDF utilities, OCR, QR codes, icons, data URIs, and base64 workflows. The launcher downloads the matching GitHub Release binary on first run and then executes it.
